{
  "name": "paws-local-calculators",
  "version": "1.4.0",
  "private": true,
  "description": "Local-only veterinary clinical calculators and workflow tools",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
